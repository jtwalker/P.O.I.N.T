<p id="notice"><%= notice %></p>

<h2><%= @picture.id %></h2>
  <%= image_tag @picture.photo.url(:medium) %> <br /><hr>

<%= link_to 'Edit', edit_picture_path(@picture) %> |
<%= link_to 'Remove', @picture, data: { confirm: 'Are you sure you want to lose a friend?' }, method: :delete, class: 'destroy' %> | 
<%= link_to 'Back', pictures_path %>
