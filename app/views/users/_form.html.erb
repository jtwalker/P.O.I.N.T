<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :provider %><br>
    <%= f.text_field :provider %>
  </div>

  <div class="field">
    <%= f.radio_button :account_type, 'STANDARD' %> 
    <%= label :account_type, 'Standard' %>
    <%= f.radio_button :account_type, 'POIMGR' %> 
    <%= label :account_type, 'Point of Interest Manager' %>
    <%= f.radio_button :account_type, 'ADMIN'%> 
    <%= label :account_type, 'Administrator' %>
  </div>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.email_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
