<!DOCTYPE html>
<html>
	<head>
    	<link href="js-image-slider.css" rel="stylesheet" type="text/css" />
    	<script src="js-image-slider.js" type="text/javascript"></script>
  	</head>
  	<body>
		<div class = "summary">
			<h1>Summary - About the POI</h1>
				<%= @poi.summary %>
		</div>
		
		<div class = "sponsor">
			<h1>Sponsor Information</h1>
				<%= @poi.sponsor_info %>
		</div>
		
		<div class = "artist">
			<h1>Artist</h1>
				<%= @poi.artist_info %>
		</div>
		
		<div class = "pictures">
			<h1>Pictures - UNDERCONSTRUCTION</p>
				<div id="slider">
			        <%= image_tag("wolfie.png", :alt => "slide 1", height: '150', width: '150') %>
			        <%= image_tag("wolfie.png", :alt => "slide 2", height: '200', width: '200') %>
			        <%= image_tag("wolfie.png", :alt => "slide 3", height: '175', width: '175') %>
			    </div>
		</div>

		<div class = "comments">

			<div class = "add_comment"
				<% if current_user %>
					<h2>Add a comment:</h2>

					<%= form_for([@poi, @poi.comments.build]) do |f| %>
					  <p>
					    <%= f.label :body %><br />
					    <%= f.text_area :body %>
					  </p>

						<%= f.hidden_field :user_id, :value => current_user.id %> 

					  <p>
					    <%= f.submit %>
					  </p>
					<% end %>

				<% end %>
			</div>

			<h2>Comments</h2>
			<% @poi.comments.each do |comment| %>
				<% if comment.body  %>	
					<p>
				    	<strong><%= get_user(comment.user_id).name %></strong>
				    	<%= comment.body %>
				 	</p>
				  
				  	<% if current_user && current_user.id == comment.user_id %>
						<%= link_to 'Delete Comment', [comment.point_of_interest, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
				  	<% end %>
				<% end %>
			<% end %>

		</div>
	</body>

</html>