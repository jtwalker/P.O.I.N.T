<!DOCTYPE html>
<html>
	<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
	<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>

	<head>
		<!--[if lt IE 9]>
    		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  		<![endif]-->
		<%= stylesheet_link_tag "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
  		<%= csrf_meta_tags %>
  		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		
		<title>POINT</title>
	</head>
	
	<div class = "container-fluid">
		
		<nav class="navbar">
  			<div class="navbar-inner">
  				<div class="container">
					<a class="brand"><%= link_to( image_tag("POINT-Logo-1.jpg"), "/") %></a>
			
					<ul class = "nav pull-right">
						<% if poimgr? %>
							<li class = "divider-vertical"><%= link_to "POIs", controller: "point_of_interests", action: "index"%></li>
						<% end %>
						
						<li class = "divider-vertical"><%= link_to "Rules", controller: "main", action: "rules"%></li>
						<li class = "divider-vertical"><%= link_to "Contact Us", controller: "contact_us_messages", action: "new" %></li>
						
						<% if admin? %>
							<li class = "visbile-desktop divider-vertical"><%= link_to "Users", controller: "users", action: "index"%></li>
							<li class="visible-desktop divider-vertical"><%= link_to "Admin", controller: "admin", action: "index"%></li>
						<% end %>
					    
					    <% if current_user %>
					    	<li class = "divider-vertical"><%= link_to "Profile", user_path(current_user) %></li>
					    <% end %>
					    
					    <% if current_user %>
		    				<li class = "divider-vertical">Signed in as <strong><%= current_user.name %></strong>!</li>
		    				<li class = "divider-vertical"><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
		  				<% else %>
		  					<li class = "divider-vertical"><%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %> </li>
							<li class = "divider-vertical"><%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %> </li>
						<% end %>

					</ul>
				</div>
			</div>
				
		</nav><!-- closes nav -->
		
		<%= bootstrap_flash %>

		<div class="body">
			
			<%= yield %>
			
		</div><!-- closes body -->
		
		<script type='text/javascript'>
			handler = Gmaps.build('Google');
			handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
	  			markers = handler.addMarkers(<%=raw @hash.to_json %>);
	  			handler.bounds.extendWith(markers);
	  			handler.fitMapToBounds();
			});
		</script>
	</div><!-- closes wrapper_all -->

</html>
